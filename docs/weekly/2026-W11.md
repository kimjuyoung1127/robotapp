# 주간 롤업 2026-W11

## 완료 항목
- GameLab-style 제품 문서 운영 프로세스를 `robotapp2`에 이식
- canonical product docs 3종(`PRD`, `WIREFRAME`, `PRODUCT-ROADMAP`)과 `PRODUCT-DOC-BOARD`를 추가
- status/skill/integrity/automation 계층에 제품 문서 drift 규칙을 연결
- `docs/ref/product/` branching 구조를 추가하고 root canonical 문서를 summary 문서로 재정리
- `guided-lesson`, `robot-library`, `sandbox` leaf 문서를 실제 화면 단위 계약으로 확장
- `concept-to-ui-map`를 공개 robotics reference 기반 concept 묶음으로 확장
- `open-robotics-reference-pack`, `llm-teaching-strategy`, `mobile-release-checklist`를 추가
- `robotics-reference-to-lesson` skill을 추가하고 기존 6개 skill에 robotics 교육 규칙을 흡수
- `current-feature-checklist`를 추가해 현재 기능/없는 기능/우선 추가 기능을 한 문서로 정리
- `Lesson 0~3` 초보자 진입 트랙을 문서에 반영해 수학 이전의 직관 단계와 Core Track을 분리
- root canonical(`PRD/WIREFRAME/PRODUCT-ROADMAP`)과 `ai-context/project-context`도 beginner track 기준으로 정합화
- 공개 robotics reference를 기준으로 frame/pose notes, pick foundation state machine, robot metadata 확장 문서를 추가
- 에셋 수집 체크리스트와 무료 소스/검색어 가이드를 추가
- 내부 큐레이션 프리팹 44개에 대한 hierarchy instantiate 스모크 테스트와 validation report를 추가
- `AGENTS.md`와 `CLAUDE.md`에 beginner track, current feature checklist, asset curation/validation 라우팅을 보강
- `docs/ref/phase5-implementation-plan.md`를 `AGENTS.md` / `CLAUDE.md` 시작 경로와 task routing에 연결하고 `CLAUDE.md` 현재 상태에 `Phase 5: Planning Complete`를 반영
- `docs/ref/phase5-implementation-plan.md`에 Phase 5 코드 검수 결과를 반영해 snapshot 위치, track-aware resume, update cause, target marker serialization, compare trail 구조를 보정
- Phase 5 상태/범위를 `PROJECT-STATUS`, `PHASE-EXECUTION-BOARD`, `current-feature-checklist`, `milestone-backlog`, `USER-FLOW`, `tutor-step-plan`과 정합화
- Phase 5 실행 순서를 `기반층 우선`으로 재정렬하고, 문서 선행 sync 후 phase별 테스트/self-review/commit 루프를 고정
- Phase 5A runtime foundation과 Phase 5B track-aware step foundation을 구현하고 EditMode `53/53 passed`를 확인
- Phase 5C 공통 input/visualization 인프라를 구현하고 PlayMode `Phase5CommonVisualsSmokeTests 4/4 passed`를 확인

- Phase 5D: Why It Moved explanation layer 구현 완료
- Phase 5E: Beginner Lesson L0~L3 통합 완료
- Phase 5F: Robot Library MVP 구현 완료
- Home/Continue Hub와 Resume/Session Context를 차기 단일 재진입 흐름으로 묶어 문서 정합화
- Sandbox Polish와 Tablet 4DOF Input Usability를 병행 P0 우선순위로 재정렬
- asset subset Git tracking을 기능 후순위에서 P0 운영 과제로 승격
- WIREFRAME과 PRODUCT-ROADMAP을 Home/Sandbox/재진입 흐름 기준으로 업데이트 (v1.2.0)
- P0/P1 재우선순위: 차기 우선순위를 `Home/Continue Hub → Sandbox polish → Resume/Session Context → Tablet 4DOF → Snapshot Lite`로 확정
- 온보딩 `건너뛰기` 정책을 late-step direct jump 대신 `Home / Continue Hub`로 연결하도록 수정
- Onboarding QA/디버그용 전역 페이지 이동 바를 `Canvas` 직하위 런타임 nav(`OnboardingDebugNav`)로 전환해 정적 TopBar 경로 실패 시에도 즉시 이동 가능하게 조정
- Onboarding/nav 렌더 문제를 기준으로 `scene-ui-visibility` 확장 및 `onboarding-debug-nav` 스킬화 후보를 도출
- Robot Library 3D showroom의 런타임 렌더 경로(`RawImage + RenderTexture + showroom camera`)를 복구하고 카드 재빌드/스크롤 초기화 충돌을 보정
- Robot Library showroom을 `hero + 주변 preview` 구조로 확장해 `2DOF RR + SCARA + 6DOF placeholder` 초기 3대 동시 프리뷰를 확인

## Phase 진행률
- Phase 0~4 완료 유지
- Phase 5: `5A~5F complete`, 잔여 Ready(Home/Sandbox/Resume/Tablet/Snapshot/Replay/Constraint/InstructorDemo/Templates 3DOF/6DOF) 대기 중
- Phase 6 CI/CD Hold 상태 유지
- Product Docs Governance: WIREFRAME/PRODUCT-ROADMAP QA 단계 (architecture-diagrams 동기화 1건 manual_required)

## 다음 주 목표
- `architecture-diagrams.md`를 WIREFRAME 2026-03-12 변경(Home/Sandbox/재진입 흐름) 기준으로 업데이트
- Phase 5 잔여 P0 항목 순차 구현: Home/Continue Hub → Sandbox Polish → Resume/Session Context → Tablet 4DOF Input Usability → Snapshot Lite
- CI 워크플로우 실주행 1회 검증 (Phase 6 Hold 해제 조건 충족)
- `current-feature-checklist` 기준으로 P0 구현 범위 구체화
- Android 태블릿 internal build 체크리스트를 실제 빌드 준비 항목과 매칭
- 공식 레퍼런스 기반으로 `frame-pose bridge`, `pick state machine`, `robot metadata badge` 구현 우선순위 정리
- Robot Library showroom 후속 프리뷰 확장(`FANUC`, `IGUS`)과 페이지/캐러셀 규칙, Sandbox 진입 연동을 우선 검증
- FR5 donor preview와 RobotControl control prefab을 분리해 로봇 사용 기준을 명확화
- `RobotControl.unity`와 FR5 제어 콘솔 baseline을 추가하고, Build Settings index 6에 연결
- `SceneCameraDirector`로 gameplay camera profile을 중앙 관리하도록 정리
