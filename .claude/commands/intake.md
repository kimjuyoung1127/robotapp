# /intake

새 요청을 받았을 때 먼저 `meta/task-intake-router`를 적용한다.

## Steps

1. 요청을 `implement`, `verify`, `doc-sync`, `review`, `impact-map`, `handoff` 중 어디에 가까운지 분류한다.
2. 다음에 읽을 문서 1~3개를 고른다.
3. 이번 세션에서 쓸 스킬 1~2개를 고른다.
4. first action과 blocker를 한 줄씩 남긴다.
