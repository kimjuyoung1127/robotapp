// Folder: PointMove - thin anchor for saved point and sequence orchestration partials.
using KineTutor3D.Math;

namespace KineTutor3D.App.Fairino
{
    // PointMove runtime responsibilities are split into sequence, home-loop, mixed-live, and preview partials.
    // Joint/TCP low-level apply helpers stay in JointControl/TcpControl and approval state stays in Shared.
    public sealed partial class RobotControlV3RuntimeController
    {
    }
}
