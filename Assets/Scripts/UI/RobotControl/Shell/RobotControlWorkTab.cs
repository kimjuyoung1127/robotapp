// Folder: UI - HUD/view components only; no kinematics logic.
namespace KineTutor3D.UI
{
    /// <summary>
    /// WorkPanelHost 아래에서 전환되는 V2 작업 탭입니다.
    /// </summary>
    public enum RobotControlWorkTab
    {
        EasyMotion = 0,
        TcpJog = 1,
        JointJog = 2,
        PointMove = 3,
        Teaching = 4,
    }
}
